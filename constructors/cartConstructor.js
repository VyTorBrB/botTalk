class CartItems {
    constructor(params) {
        this.params = params,
        this.total = total
    }
    
    setTotal() {
        let total = params.total
        
        total = params.total
    }
}